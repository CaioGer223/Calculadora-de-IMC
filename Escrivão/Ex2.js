/*Crie Variavel que converte dados digitados */
const teclado = require("prompt-sync")({sigint:true});
/*Recebe Dados */

let cont = 0;

    do{
        cont++;
        console.log(cont);
        
}while(cont < 100)

